code:

def is_leap(yrs):
   if(1900 <= yrs <= 100000):
        if((yrs % 400 == 0) or (yrs % 4 == 0 and yrs % 100 != 0)):
            return True
        else:
            return False

----------------------------------------------------------------------------------------

sample input:

1990

sample output:

False
